import React from 'react';

const SQLJoins = () => {
    return (
        <div>
            <h1>SQL JOINS</h1>
            <p>This component displays SQL JOINs visualizations and examples.</p>
            <!-- add SQL JOINs code or examples here -->
        </div>
    );
};

export default SQLJoins;